name: vcp-dev

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: vcp
      POSTGRES_PASSWORD: vcp
      POSTGRES_DB: vcp
    ports:
      - "5432:5432"

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
